name: Serok
on: [push]

jobs:
  serok-code:
    runs-on: ubuntu-latest
    container:
      image: rsneh/serok:latest
      env:
        ACTOR: ${{ github.actor }}
        REPOSITORY: ${{ github.repository }}
        SECODE_TOKEN: ${{ env.SECODE_TOKEN }}
    steps:
        - uses: actions/checkout@v2
        - name: Start Serok
          uses: docker://rsneh/serok:latest


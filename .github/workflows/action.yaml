name: Serok
on: [push]

jobs:
  serok-code:
    runs-on: ubuntu-latest
    container:
      image: rsneh/serok:latest
    steps:
        - uses: actions/checkout@v2
        - name: Start Serok
          uses: docker://rsneh/serok:latest

